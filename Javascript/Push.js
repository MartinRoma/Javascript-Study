const arr = [1,2,3];
arr.push(4);
alert (arr[3]);
/* Al igual que push agrega un elemento al final del array, unshif agrega un elemento al inicio,
shif elimina el primero y pop elimina el ultimo */
